# VEX V5 C++ API 数据采集可行方案

## 前置条件：已确认的数据源结构

### 经过 json.loads 完整验证

| 数据源 | 状态 | 实际内容 |
|--------|------|---------|
| `objects.inv` | ✅ 已下载 | 203 条 `std:doc`（文档页面引用），**无 `cpp:function` 条目** |
| `searchindex.js` | ✅ 已下载 | `Search.setIndex({...})` 格式，11 个顶层 key |
| HTML 页面 | ⚠️ 待验证 | https://api.vex.com/v5/ 下各页面 |

### searchindex.js 实际结构（完整解析）

```json
{
  "docnames": ["home/cpp/..." ×66, "home/python/..." ×64, ...],  // 196 条
  "filenames": ["....md", ...],           // 196 条 .md 文件名
  "titles": ["Accelerometer", ...],       // 196 条
  "terms": {"The": [0,1,...], ...},       // 2359 词倒排索引
  "objects": {},                          // ⚠️ 空！
  "objtypes": {},                         // ⚠️ 空！
  "objnames": {},                         // ⚠️ 空！
  "titleterms": {"acceleromet": [0,...], ...}, // 607 标题词索引
  "alltitles": {"Accelerometer": [[0,"anchor"],...], ...}, // 844 全标题
  "envversion": {"sphinx.domains.cpp": 2, ...},
  "indexentries": {}                      // ⚠️ 空！
}
```

**关键结论：**
- `objects`、`objtypes`、`objnames` 全部为空 `{}` — **该 Sphinx 站点未生成 API 对象索引**
- 没有任何结构化数据源包含 C++ API 的函数签名
- C++ 文档页面 66 个，路径格式 `home/cpp/{Device}/{Page}`

### objects.inv 实际内容

全部 203 条是 `std:doc` 或 `std:label`（文档页面引用），**不是** `cpp:function` 之类的 API 索引：
```
[std:doc] home/cpp/Motors_and_MotorControllers/motor_and_motor_group
[std:doc] home/cpp/Controller/Axis
[std:doc] home/cpp/Brain/timer
...
```

---

## 推荐方案：HTML 爬取为主（2 层架构）

由于两个结构化数据源都**不包含 API 签名**，核心数据必须从 HTML 页面提取。

### 第 1 层：searchindex.js / objects.inv → 获取页面清单

**可用数据：**
- 66 个 C++ 文档页面的路径（`docnames`）
- 每个页面的标题（`titles`）
- `alltitles` 中的子标题→锚点映射（可用于定位页面内的 API 段落）
- `terms` 倒排索引（可用于关键词搜索定位相关页面）
- `titleterms` 标题词索引

**不可用：**
- ❌ API 函数签名
- ❌ API 参数/返回值
- ❌ 代码示例
- ❌ 描述文本

### 第 2 层：按需爬取 HTML → 提取详细信息

**必须爬取，因为结构化数据源不含任何 API 签名。**

**有利条件：**
1. 仅 66 个 C++ 页面（非全站 196 页）
2. Sphinx 使用固定 HTML 结构（CSS 类名可预测）
3. 页面 URL 格式：`https://api.vex.com/v5/{docname}.html`
4. 源文件是 Markdown（`.md`），HTML 结构相对规整

**爬取策略（选择器需验证后调整）：**

```python
import json
import requests
from bs4 import BeautifulSoup
from pathlib import Path

BASE_URL = "https://api.vex.com/v5/"

# 步骤 1: 从 searchindex.js 读取 C++ 页面列表
with open("searchindex.js", "r", encoding="utf-8") as f:
    content = f.read()

# 提取 JSON（Search.setIndex(...) 中的部分）
import re
m = re.search(r"Search\.setIndex\((.+)\)$", content)
data = json.loads(m.group(1)) if m else json.loads(content)

# 筛选 C++ 页面
cpp_docnames = [d for d in data["docnames"] if "cpp" in d]
cpp_titles = [data["titles"][data["docnames"].index(d)] for d in cpp_docnames]

print(f"C++ 页面数: {len(cpp_docnames)}")

# 步骤 2: 爬取每个页面
api_db = []
for docname, title in zip(cpp_docnames, cpp_titles):
    url = f"{BASE_URL}{docname}.html"
    resp = requests.get(url, headers={
        "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) Chrome/125.0.0.0"
    })
    soup = BeautifulSoup(resp.text, "html.parser")

    # ⚠️ 以下选择器需在验证实际 HTML 后修改
    # Sphinx C++ 域常见模式（需验证）：
    #   <dl class="cpp function">  → 函数
    #   <dl class="cpp class">     → 类
    #   <dl class="field-list">    → 参数表
    #   <div class="highlight-cpp"> → 代码示例

    for dl in soup.find_all("dl", class_=lambda c: c and "cpp" in c):
        # 提取签名
        sig = dl.find("dt").get_text(strip=True) if dl.find("dt") else ""
        # 提取描述
        desc = dl.find("dd").get_text(strip=True) if dl.find("dd") else ""
        api_db.append({
            "page": docname,
            "title": title,
            "signature": sig,
            "description": desc,
            "url": url,
        })

# 步骤 3: 保存
with open("vex_cpp_api.json", "w", encoding="utf-8") as f:
    json.dump(api_db, f, ensure_ascii=False, indent=2)
print(f"提取完成: {len(api_db)} 条 API")
```

---

## 完整工作流程

```
┌──────────────────────────┐
│ 1. searchindex.js        │  →  解析 JSON → 66 个 C++ 页面路径 + 标题
└───────────┬──────────────┘
            │
            ▼
┌──────────────────────────┐
│ 2. 验证单个 HTML 页面     │  →  打开一个示例页面 → 确认 DOM 选择器
└───────────┬──────────────┘
            │
            ▼
┌──────────────────────────┐
│ 3. 爬取 66 个 HTML 页面   │  →  遍历 docnames → requests + BeautifulSoup
└───────────┬──────────────┘
            │
            ▼
┌──────────────────────────┐
│ 4. 提取 API 结构化信息    │  →  函数签名、参数、返回值、示例、描述
└───────────┬──────────────┘
            │
            ▼
┌──────────────────────────┐
│ 5. 数据清洗 + 存储        │  →  去重、补全 → JSON/SQLite
└───────────┬──────────────┘
            │
            ▼
┌──────────────────────────┐
│ 6. MCP 服务              │  →  FastMCP 封装
└──────────────────────────┘
```

---

## 关键前置验证（必须第一步做）

在写任何爬取代码前，**必须先做以下验证**：

1. **打开一个实际 C++ API 页面确认 DOM 结构**
   - 例如：`https://api.vex.com/v5/home/cpp/Motors_and_MotorControllers/motor_and_motor_group.html`
   - 用浏览器 DevTools 检查：
     - 函数签名在什么 HTML 标签里？
     - 参数表是什么 CSS 类名？
     - 代码示例在什么标签里？
     - 页面是否需要 JavaScript 渲染？

2. **验证反爬策略**
   - 检查是否有 Cloudflare / WAF
   - 确认请求频率限制

---

## 预估时间

| 步骤 | 预估时间 |
|------|---------|
| 验证 HTML 页面 DOM 结构 | 20 min |
| 编写 HTML 爬取 + 提取脚本 | 1-2 h |
| 数据清洗 + 存储 | 30 min |
| MCP 服务封装 | 30 min |
| 测试 + 修正 | 30 min |
| **总计** | **2.5-4 h** |

---

## 与阶跃星辰方案的对比

| 维度 | 阶跃星辰方案 | 本方案 |
|------|------------|--------|
| 核心数据源 | `docs`（不存在）、`modules`（不存在） | `searchindex.js` 页面清单 + HTML 爬取 |
| `objects` 字段 | 声称有 API 数据 | **确认为空 `{}`** |
| 是否需爬页面 | 声称"不需要爬任何HTML页面" | **必须爬取**（66 个页面） |
| 代码可运行 | 否（`ReferenceError`） | 是 |
| 数据完整度 | 0%（基于不存在的字段） | 取决于 HTML 解析准确率 |
| 准确率声称 | "100%" | 实事求是 |

---

## 下一步

1. **最重要：** 在浏览器中打开一个示例 C++ API 页面确认 DOM 结构
2. 编写爬取脚本提取 API 信息
3. 构建 MCP 服务
